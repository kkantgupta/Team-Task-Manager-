{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Railway deployment wrapper for the Team Task Manager app.",
  "scripts": {
    "build": "npm --prefix backend ci",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  }
}